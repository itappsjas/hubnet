# Database
DATABASE_URL="mysql://user:password@localhost:3306/database_name"

# Email Configuration (SMTP)
# You can use either the internal SMTP server or Google SMTP

# Option 1: Internal SMTP Server (Based on your PHP config)
SMTP_HOST=50.1.1.6
SMTP_PORT=25
SMTP_SECURE=false
SMTP_USER=
SMTP_PASSWORD=
SMTP_TIMEOUT=30000

# Option 2: Google SMTP (if you want to use Gmail)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password

# Sender Email
EMAIL_FROM=noreply@ptjas.co.id

# WhatsApp Configuration (WHAPI)
# API Key (authorization) - Wajib banget! Format: UnoDIn8ROY6AgbgX5dtIyGPokc7OXfE1
WHAPI_TOKEN=UnoDIn8ROY6AgbgX5dtIyGPokc7OXfE1
# Alternative: WHAPI_API_KEY (if using different env var name)

# WhatsApp Base URL (default: https://gate.whapi.cloud)
# WHAPI_BASE_URL=https://gate.whapi.cloud

# Verified sender phone number (optional, for some Whapi configurations)
# WHAPI_SENDER_PHONE=6281234567890

# WhatsApp Options
# USE_WHATSAPP=true (to enable WhatsApp sending)
# USE_WHATSAPP_ONLY=true (to send only WhatsApp, skip email)

# Old Resend Configuration (keep for backward compatibility if needed)
RESEND_API_KEY=your-resend-api-key
RESEND_FROM_EMAIL=noreply@ptjas.co.id

# Base URL for the application (used for email templates and public assets)
# For production: https://hubnet.jas.com
# For development: http://localhost:3000
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Next Auth Secret (if using NextAuth)
NEXTAUTH_SECRET=your-secret-key
NEXTAUTH_URL=http://localhost:3000

# JWT Secret (for authentication)
JWT_SECRET=your-jwt-secret


